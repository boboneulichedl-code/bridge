{
  "name": "@bridge/cursor-adapter",
  "version": "1.0.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p . && node scripts/copy-registry.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@bridge/shared": "*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.4.0",
    "vitest": "^3.0.0"
  }
}

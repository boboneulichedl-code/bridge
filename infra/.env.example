# Bridge Production Environment
NODE_ENV=production
BRIDGE_API_HOST=0.0.0.0
BRIDGE_API_PORT=3847
# Generate on deploy: openssl rand -hex 32
BRIDGE_API_TOKEN=
BRIDGE_MAX_ACCESS=1
BRIDGE_PUBLIC_URL=https://bridge.chatpilot.link

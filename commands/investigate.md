# Untersuchung

Startet eine Multi-Source-Untersuchung über alle konfigurierten MCP-Plugins.

```bash
bridge investigate "Login-Fehler in Production"
bridge route errors "500 on /api/auth"
```

Tastenkürzel: `Ctrl+Alt+Shift+I`

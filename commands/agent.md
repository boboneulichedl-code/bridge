# Agent-Modus

Starte eine Agent-Aufgabe mit vollem Tool-Zugriff (Dateien, Terminal, Suche).

Nutze den Bridge-Befehl **Bridge: Prompt senden** (`Ctrl+Alt+P`) oder die CLI:

```bash
bridge prompt "DEINE AUFGABE HIER" --mode agent
```

Im Composer: `Shift+Tab` bis **Agent** aktiv ist.

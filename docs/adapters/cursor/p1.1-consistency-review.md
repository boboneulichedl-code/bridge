# Bridge Cursor Adapter — P1.1 Consistency Review

**Status:** P1.1 (Review)  
**Datum:** 2026-05-27  
**Scope:** Formale Konsistenzprüfung aller P1-Deliverables gegen P0-Quellen und Runtime-Registry  
**Geänderte Dateien in P1.1:** nur dieses Dokument (+ 4 weitere `p1.1-*.md`) — **keine** P1/P0/Proposal/Code-Änderungen

---

## 1. Prüfmethodik

### Gelesene Quellen (vollständig bzw. stichprobenartig mit grep/Abgleich)

| Gruppe | Dateien | Ergebnis |
|--------|---------|----------|
| P0 Docs | `p0-completion-report.md`, `poc-v1-spec.md`, `phase-0-implementation-plan.md`, `p0-manual-testing.md` | vorhanden |
| P1 Docs (7) | `p1-analysis-report.md` … `p1-review-checklist.md` | vorhanden |
| P1 Proposals | `p1-cursor-self-structure.proposal.json`, `p1-ui-modules.proposal.json` | vorhanden, JSON gültig |
| P0 Registry | `poc-v1-actions.json`, `poc-v1-commands.json`, `poc-v1-terminal-whitelist.json`, `poc-v1-permissions.json` | vorhanden |
| Contract | `shared/src/cursor-contract.ts` (`P0_ACTION_IDS`) | Abgleich 10 IDs |

### Automatisierte Checks (ohne Build)

| Check | Ergebnis |
|-------|----------|
| `JSON.parse` beider Proposal-Dateien | OK |
| `rollbackAvailable": true` im Repo (json/md/ts) | **0 Treffer** |
| Self-Structure: `actions` mit `status: "implemented"` | **10** (identisch zu P0) |
| Self-Structure: `actions` mit `status: "proposed"` | **6** |
| UI-Proposal: `modules.length` | **18** |
| Root `proposalOnly` / `runtimeActive` (UI-Proposal) | `true` / `false` |
| `load-registry.ts` lädt nur `poc-v1-*.json` | bestätigt (Zeilen 29–35) |

---

## 2. Checkliste Aufgabe A (User §3)

| # | Kriterium | Ergebnis | Beleg |
|---|-----------|----------|-------|
| 1 | Genau **10 P0-Actions** als implemented markiert | **Pass** | Proposal `actions[]` (10× `implemented`); `poc-v1-actions.json` (10 `actionId`); `P0_ACTION_IDS` in `cursor-contract.ts` |
| 2 | Alle neuen Actions nur **proposed/future** | **Pass** | 6 proposed in Self-Structure; UI-Module `proposedFutureActions`; Domains „proposed future actions“ |
| 3 | `rollbackAvailable` nirgends fälschlich **true** | **Pass** | Repo-weites grep: 0× `"rollbackAvailable": true`; Proposals nutzen `false` bei implemented actions |
| 4 | Agent überall **Subsystem** | **Pass** | `cursor.subsystem.agent`, `primaryPurpose: false`, Domain 13, `bridge.ui.agent.prompt` `subsystem: true` |
| 5 | Cursor **erster Adapter**, nicht Gesamtsystem | **Pass** | `p1-analysis-report.md`, Self-Structure `cursorRole`, `bridgeRole` |
| 6 | Keine **monolithische** Cursor-Control-UI | **Pass** | `p1-ui-modules.proposal.json` → `viewCompositionProposal.forbidden: "monolithic-cursor-control-page"`; mehrfach in P1-Docs |
| 7 | Phasen **P0 / P0.1 / P1 / P1.1 / P2 / later** getrennt | **Pass** | `p1-phase-boundaries.md`; Proposal-Phasen an Actions/Areas |
| 8 | Designrules/Tokens **TBD**, nicht erfunden | **Pass** | UI-Proposal `designrulesStatus: "missing-in-repo"`, leere `requiredDesignTokens`/`requiredComponents`, `spec: "TBD"` |
| 9 | Interface Reader / GUI / OCR / Vision **nicht** als P1-Implementierung | **Pass** | `p1-interface-reader-concept.md` Titel + Domain 17; `unsupportedAreas` gui-automation, vision-ocr |
| 10 | Proposals `proposalOnly: true`, `runtimeActive: false` | **Pass** | Beide JSON-Roots + jedes UI-Modul |
| 11 | Proposals **nicht** als Runtime-Registry dargestellt | **Pass** | Beschreibungstexte, `load-registry.ts`, `sources[].type: runtime-registry` zeigt auf `poc-v1-*` only |

**Hinweis zu Kriterium 1:** Zusätzlich zu 10 Actions sind **4 capability methods** und **1 event** (`cursor.action.done`) als `implemented` markiert — korrekt für P0-Infrastruktur, aber in Reviews von „10 implementierte Actions“ **getrennt** halten (siehe C2).

---

## 3. Cross-File-Matrix (Kern)

### 10 P0-Actions

| actionId | poc-v1-actions | Self-Structure | control-domains | UI-Modul (supportedActions) |
|----------|------------------|----------------|-----------------|-----------------------------|
| `cursor.ide.status.get` | ja | implemented | Domain 1 | `bridge.ui.cursor.status` |
| `cursor.ide.workspace.open` | ja | implemented | Domain 2 | `bridge.ui.workspace` |
| `cursor.ide.fs.mkdir` | ja | implemented | Domain 3 | `bridge.ui.file.create` |
| `cursor.ide.fs.write` | ja | implemented | Domain 3 | `bridge.ui.file.create` |
| `cursor.ide.settings.get` | ja | implemented | Domain 5 | `bridge.ui.settings` |
| `cursor.ide.settings.set` | ja | implemented | Domain 5 | `bridge.ui.settings` |
| `cursor.ide.extension.install` | ja | implemented | Domain 6 | `bridge.ui.extension.install` |
| `cursor.ide.terminal.run` | ja | implemented | Domain 7 | `bridge.ui.terminal.command` |
| `cursor.ide.command.execute` | ja | implemented | Domain 8 | `bridge.ui.ide.command` |
| `cursor.agent.prompt.send` | ja | implemented | Domain 13 (Subsystem) | `bridge.ui.agent.prompt` |

### Domains ohne dediziertes UI-Modul (bewusst/indirekt)

| domainId | Abdeckung |
|----------|-----------|
| `cursor.domain.editor` | `bridge.ui.editor` (read-only/indirekt P0) |
| `cursor.domain.panels` | `bridge.ui.panels` (via command.execute) |
| `cursor.domain.git` | `bridge.ui.git.status` (indirekt) |
| `cursor.domain.problems` | `bridge.ui.problems` (indirekt) |
| `cursor.domain.tasks` | terminal + ide.command Module (kein `bridge.ui.tasks`) |
| `cursor.domain.audit` | `bridge.ui.audit.history` + API GET |
| `cursor.domain.permissions` | Querschnitts-UI-Module |
| `cursor.domain.version` | `bridge.ui.version.info` |
| `cursor.domain.interface-reader` | kein UI-Modul (Konzept/later) |

### Kommunikationskanäle

`p1-communication-model.md` dokumentiert **16** Kanäle; Self-Structure `communicationChannels` listet **11** (Teilmenge der wichtigsten). **Kein Widerspruch** — Self-Structure ist aggregiert; vollständige Liste bleibt im MD.

---

## 4. Abweichungen P1 ↔ P0 Registry

| Thema | P0 Runtime | P1-Dokumentation / Proposal | Bewertung |
|-------|------------|----------------------------|-----------|
| Action-Anzahl | 10 | 10 implemented | konsistent |
| Commands Allowlist | 7 | 7 in `poc-v1-commands.json`, in Domains/Commands genannt | konsistent |
| Terminal Whitelist | 6 exakt | 6 in JSON, in Domain 7 | konsistent |
| Permission `fs-delete` | in `poc-v1-permissions.json` | keine P0-Action | **bekannt**, in Self-Structure als `P0-registry-only` dokumentiert |
| `rollbackPossible: true` (Registry) vs `rollbackAvailable: false` | poc-v1-actions | P1 erklärt Restore P0.1 | **korrekt** laut P0-Plan |
| Registry-Version | `poc-v1.1.0` | in Self-Structure `sources` | konsistent |

---

## 5. Minor-Inkonsistenzen (dokumentiert, nicht gefixt)

| ID | Befund | Schwere | Empfohlene Korrektur (nur bei expliziter Freigabe an P1-Dateien) |
|----|--------|---------|------------------------------------------------------------------|
| **C2** | „implemented“ umfasst auch capabilities + event — kann mit „10 Actions“ verwechselt werden | Info | In `p1-analysis-report.md` oder Proposal: Legende „implemented entity types“ |
| **C3** | `uiModuleCandidates` nur 3 Einträge, UI-Proposal hat 18 Module | Minor | Self-Structure: alle 18 referenzieren oder Hinweis „see full p1-ui-modules“ |
| **C4** | Domain `phase: "P1"` vs Action `phase: "P0"` — gleiches Feld, andere Semantik | Minor | Vor Promotion: `modelPhase` vs `implementationPhase` splitten |
| **C5** | `futureAreas.poc-v1-ui-modules-runtime.phase: "P1.1"` klingt nach Promotion in P1.1 | Minor | Text auf „nach P1.1-Review, Promotion in P2“ präzisieren |
| **C6** | `cursor.domain.tasks` ohne `bridge.ui.tasks` | Info | In `p1-control-domains.md` bereits als indirekt beschrieben — ausreichend |
| **C7** | `p1-communication-model.md` Zeile „P1 implementiert keine Kanäle“ vs Kanal-Tabelle „currentPhase P0 implementiert“ | Info | Formulierung: „P1 implementiert keine **neuen** Kanäle“ |

**Keine kritischen Fehler** (falsche implemented-Actions, rollbackAvailable true, fehlende proposalOnly-Flags).

---

## 6. Gesamturteil

| Aspekt | Urteil |
|--------|--------|
| P1 intern konsistent | **Ja** — mit dokumentierten Minor-Punkten C2–C7 |
| P1 vs P0 Registry | **Ja** — 10 Actions deckungsgleich |
| Bereit für P1.1-Abschluss | **Ja** |
| Kritische Fixes vor P2 | **Keine** in P1-Inhalten; P2 blockiert durch **fehlende Designrules** (siehe [p1.1-decision-backlog.md](p1.1-decision-backlog.md)) |

---

## Verwandte P1.1-Dokumente

- [p1.1-proposal-review.md](p1.1-proposal-review.md)
- [p1.1-naming-review.md](p1.1-naming-review.md)
- [p1.1-decision-backlog.md](p1.1-decision-backlog.md)
- [p1.1-review-checklist.md](p1.1-review-checklist.md)
